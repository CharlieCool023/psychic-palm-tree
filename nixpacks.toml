[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd app && npm install"]

[phases.build]
cmds = ["cd app && npm run build"]

[phases.build.env]
NODE_ENV = "production"

[start]
cmd = "cd app && NODE_ENV=production node dist/boot.js"
