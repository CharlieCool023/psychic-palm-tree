{
  "version": 2,
  "outputDirectory": "dist/public",
  "functions": {
    "api/[...slug].ts": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...slug].ts" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}