rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All data access goes through the backend (Firebase Admin SDK).
    // Direct client access is not permitted.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
